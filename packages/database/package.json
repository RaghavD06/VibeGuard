{
  "name": "@maverick006/database",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "db:generate": "prisma generate --schema=../../apps/api/prisma/schema.prisma",
    "db:migrate": "prisma migrate deploy --schema=../../apps/api/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}
