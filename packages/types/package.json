{
  "name": "@maverick006/types",
  "version": "1.0.5",
  "description": "VibeGuard Types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b --force",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "prepack": "npm run build"
  },
  "dependencies": {
    "typescript": "^5.0.0"
  },
  "files": [
    "dist"
  ]
}
